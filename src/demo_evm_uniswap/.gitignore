# generated by DipDup 8.6.0

# Ignore all but root directory (gitignore specific)
*
!*/

# Add project manifest and lockfiles
!**/pyproject.toml
!**/*.lock

# Add helper symlink, README, and package markers
!demo_evm_uniswap
!README.md
!**/.keep
!py.typed

# Add Python code, but exclude caches
!**/*.py
**/.*_cache
**/__pycache__

# Add configs, queries and SQL
!**/*.graphql
!**/*.json
!**/*.sql
!**/*.yaml
!**/*.yml

# Add ignore files and build recipes
!.gitignore
!.dockerignore
!**/Dockerfile
!**/Makefile