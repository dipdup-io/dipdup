database:
  kind: postgres
  host: db
  port: 5432
  user: ${POSTGRES_USER:-dipdup}
  password: ${POSTGRES_PASSWORD:-changeme}
  database: ${POSTGRES_DB:-dipdup}
  schema_name: tezos_domains_big_map

hasura:
  url: http://hasura:8080
  admin_secret: ${ADMIN_SECRET:-changeme}
