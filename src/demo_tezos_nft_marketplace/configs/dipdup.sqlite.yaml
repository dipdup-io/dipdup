# generated by DipDup 8.5.0
database:
  kind: sqlite
  path: ${SQLITE_PATH:-/tmp/demo_tezos_nft_marketplace.sqlite}

sentry:
  dsn: ${SENTRY_DSN:-''}
  environment: ${SENTRY_ENVIRONMENT:-''}

prometheus:
  host: 0.0.0.0
  port: 8000

api:
  host: 0.0.0.0
  port: ${API_PORT:-46339}

mcp:
  host: 0.0.0.0
  port: ${MCP_PORT:-9999}
  api_url: http://dipdup:${API_PORT:-46339}