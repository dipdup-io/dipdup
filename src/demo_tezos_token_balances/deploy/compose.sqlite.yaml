# generated by DipDup 8.4.3

name: demo_tezos_token_balances

services:
  dipdup:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    command: ["-C", "sqlite", "run"]
    restart: always
    env_file: .env
    ports:
      - 46339
      - 9000
    volumes:
      - sqlite:${SQLITE_PATH:-/tmp/demo_tezos_token_balances.sqlite}

volumes:
  sqlite: